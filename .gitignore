# Build artifacts
/.build/
/.out/
/.downloads/
/.support/
/.kibuild/

# vcpkg (auto-cloned during build if not configured)
/vcpkg/

# Environment secrets
.env

# settings.json
/settings.json

# macOS
.DS_Store